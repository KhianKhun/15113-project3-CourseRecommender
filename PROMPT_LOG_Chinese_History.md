This is the orginal Prompt. Over a half of my prompt are used in Chinese, and I prepare to cooperate with my friend in the future to complete this project, so I provide a Chinese Version.

The normal PROMPT_LOG is named "PROMPT_LOG.md"

## Overview (Tools & Models)

- AI chat / planning model: Claude Chat
- AI coding agent: Claude Code
- Runtime/dev tools used during development: Git, Streamlit, Python, pytest


# Claude Chat

-你阅读下我们之前的聊天或者项目，应该有一个聊天是涉及课程推荐的，我准备在这个任务里把这个想法的部分(关键)想法实现了

-我现在在想把语意标签与市场信号这个点丢掉。这个项目先把关键的地方实现，交互界面变成课程相关性的可视化，个性化推荐模块可以适当删除？数据采集那里变成一个可交互的模块，用户可以选择扒什么资料。你觉得这歌方向怎么样，有哪里需要优化的

-你觉得”用户手动添加课程实时插入图谱”这个替代方案感觉怎么样​​​​​​​​​​​​? 这个想法挺不错的

-这里我形容一遍我心里想的，然后你这里要扣细节，抓问题，把不清晰和交互或者逻辑不当的地方找出来。
首先我们有一堆的cmu课程描述，我们洗出来有用的描述文本和prerequest等一些必要的内容。这是数据，后面用户可以通过交互面板加入他们想要的课程描述。然后就是模块二，我们进行分析，这是技术层，我继续说交互层。用户点开交互层，应该是可以选择可视化两个页面。用户可以输入“可视化坐标”的数量，输入两个就展示一个二维图像，输入三个就展示一个三维立体图像。对于三维图像，用户可以通过鼠标进行拖拽，放大缩小(可能有)操作。图像上显示的是点，表示课程，和文本相关性。因为我们有很多纬度的坐标，所以选择2 or 3进行的空间进行投影。
我应该不会选择做前后端应用，应该就是一个在电脑上跑出来的应用。
对于先修课程，我的想法是做成两个表。用户在看完了上面我说的可视化以后，应该还有一个选项，类似什么查看pre request的图。大概是这样。对于交互界面，用户可以输入他上过或者感兴趣的课程，然后输入坐标标签，显示2d or 3d的可视化图。同时还要设计最多数量存在的点。推荐课程的话，我觉得应该是要有一个短小的功能的，就是根据语意相关的边的最高推荐几个，暂时不用考虑先修课程的限制。
或者说，我们甚至可以把先修课程这一点删掉，变成数据，用户一点击某个点，然后就会显示先修是什么。但是这样又有一个问题就是，对于差那种很多的课比如一个人想上A, 但是他在E, E到A的课程路径很乱。emmm，可能还是要加这个先修课程的图的。
最后是关于程序页面。程序页面应该有个最小尺寸能够放得下整个交互界面和可视化界面（这两个功能大概率是放在一起，不过后面需要细化），然后当窗口太小的时候用户就需要拖动窗口右边和下边的滚轮条控制位置这种感觉。
你觉得有什么问题，什么细节现在需要扣一下。
还有，你觉不觉得我8小时在你和Claude code的辅助下完不成？

-哦，你说得对哦，可以使用PCA降纬，这样用户就不用自己选X Y Z是什么了，但这里其实还有个问题，就是会不会3维的大小不能捕捉足够的data variance? 以及，这会不会把所有的无关的点都显示出来，还是说我们下面说的过滤图解决了这个问题。
我有想到了一个事情，就是点和点之间的距离怎么解释呢，怎么定他们的距离，用什么呢？
3d交互那里，就按照你说的来，但是我想显示重要的课号，鼠标悬停才弹出你说的
用户输入课程然后可视化那里，是的应该设置一个显示K节点，然后K设置一个最大值。最好显示过滤图，我也觉得cmu有很多课，全显示不太现实。
先修课程和语意相关图确实应该独立。
最多数量存在的点和过滤图那里相关，搞一个条然后定一个最大值
程序形态那里，我没用，我做的是本地开发，terminal里面打一下命令就能运行的，不用前后端吧？给我讲讲
关于最后怎么完成这个事情，我会让claude code给我写代码，你帮我整理思路和头脑风暴，写提示词这种工作

-等等！写SPEC之前，我们还有几件事情要做，你复述一下我们做的事情或者说流程，用那种树来写(不用代码，不用画图，直接那种手打的就好)。然后最重要的事情就是构建一个结构框架。还有就是拔取的数据怎么扒，单开一个仓库，还是放在一起，数据用什么文件储存。

-语意相关图的话，选K个点，应该是K个权重最高的点，不是离得最近的点。然后可以高亮那个确实是要搞的。仓库结构在细化一点，功能再细化一点，要保证可维护性，每个py文件里代码行数不要过多（也就是不要超过800～1000这个数值），你优化下代码结构再发我。我又想到了一个大问题，Claude code一个对话的上下文窗口可能不够，你的提示词(SPEC)可能要分成几段来写，还有设定好一个公约，为了能让多个窗口共同协作

-关于k个点的逻辑，应该是有k个点用户能显示，然后程序恒定标量几个重要的课程，这个样子，如果k太大的话，用户可能会丢失方向。还有对于文件结构，我希望他能分成两个块，第一个小块是拔取数据相关的(或者平行data src也行)，第二个小块才是剩下的部分，你再改改。

-主数据文件应该在app/里面或者和data_pipeline独立的data 文件里面。然后还有潜在的gitignore。用户自己增加的数据应该作为另一个.json文件。app/的文件，我觉得根据我们的任务体量，可能代码会比较多，我们可能需要再细化一下代码。然后在提示词里面，增加一个要求，就是py代码里面不能有超过多少多少行的代码.

-Segment是作为一个独立的文件吗，还是在SPEC1.md, SPEC2.md每个文件里面都写一个。如果可以, 开始吧用方案1

-我们继续写SPEC_1_data_pipeline.md

-继续SPEC_2_core

-继续SPEC3吧

-继续SPEC_4

-http://coursecatalog.web.cmu.edu/coursedescriptions/ 你能方位这个网站吗? 我现在已经把所有代码全部写好了，你给一个SPEC1.1，就是作为SPEC1的补丁，然后所谓系别，不要写，要根据这个网站自己爬。这个链接点开之后，里面是不同院系的课，课号是XX-YYY，XX在一个院系里面是不变的，所以要自己读取，有什么细节问题吗

-你说一下ui 的 SPEC的top k的点的显示逻辑是什么

-改。课程最高的的k个点应该由用户输入的课程决定，而不是一直不变。用户输入的课程可以是多个，node 应该通过某种计算方式计算他本身和用户输入的课程的相似程度，然后排序top k。也就是所谓删除橙色的点是根据输入在边。你说一下用什么排序方法，现在的用改吗，还有这个是写成那个SPEC的更新。

-可以用平均余弦相似度, 没有输入课程时的fallback 用户没有输入任何课程时，退回到原来的纯PageRank排序显示top K。这样图在初始状态下仍然有内容可看，不会一片空白。然后给我SPEC_4_ui.md吧

-继续帮我改写SPEC，我需要用户能自己设置显示多少个重要的点，也就是表红的点。还有，现在标签逻辑变成了，只要是涂红的点都会标记标签，然后之前的标签逻辑只有在什么都不输入的时候才存在，一旦用户输入课程，那就走上面的逻辑，标红的点要加标签。这个显示多少重要的点，和显示点的数量一样，做成可以拖动的条。给我SPEC吧

-现在显示点的数量是什么逻辑, 我们是怎么选择显示哪些点的

-我说一下我想要的所谓分数，当我们把一个description投射到386维坐标后，他是一个向量，然后我们4000(课程数)的向量，比如X，Y, 然后看X Y向量的相似程度决定相似度。如果输入多个X，比如X1，X2，那大概率是通过计算X1 X2 的平均值，或者能联合两者的办法去得到一个新的变量，然后用这个新的变量和Y去比较，得到分数，或者就是只考虑这三者，看怎么得分。我们现在的算法是这个吗，不是的话，说一下，我看看哪个更加合理

-用你的方法，合并算法或者说公式用的是什么. 

-把这个写进SPEC5吧，主题是optimize

-我这里有个问题，你看看是不是推荐系统的事。这个黄点是用户选择的，他周围的点在人类逻辑看来是更加符合的，为什么这个推荐系统给推荐的课这么离谱呢，这个推荐算法是不是有问题

-你讲下这个算法是什么，怎么加权

-你给我写一个提示词，叫做SPEC_3.1_recommonder_prereq

-我给claude code section改的。你看下这个，SPEC_4.1类似于一个补丁，修复在ui上出现的问题。SPEC0是公约。开始代码吧

-这里有一个点，我想要优化一下
你看这两个课的description
15-251 Great Ideas in Theoretical Computer ScienceFall and Spring: 12 units
"This course is about how to use ..."
Prerequisites: (15-122 Min. grade C or 15-150 Min. grade C) ...
21-228 Discrete MathematicsFall and Spring: 9 units
"The techniques of discrete mathematics ..."
Prerequisites: 21-128 or 15-151 ...
你觉得这两门课的相似度有多高

-我要试试更加偏向学术的文本呢。或者我们设置新的算法，去增强这个acedemic方向的权重呢？

-pageRank是什么，简单说一下算法，带上数学公式，最后会得到什么

-现在的相似性用的完全是文本的相似度，而不是本身知识的相似度对吧，不管是PageRank得到的关注的点还是余弦相似度

-现在我想的有两个解决方向，第一个就是继续复杂化推荐算法的分数计算。第二就是引入知识标签，通过知识标签来做分数判断，而不是现在的文本相似性。你说一下这两个方向的一个可行解决方法是什么

-不过 GNN 的实现成本明显更高，需要引入 PyTorch Geometric 或者 DGL 这样的图学习框架，训练也需要有监督信号（比如"这两门课应该相似"的标注数据）。对于你现在的项目规模，这可能是过度设计。
你说说这个成本怎么高，我要实现GNN，我要做什么，大概。

-怎么定义邻居，是先修图，还是什么图，课程图有个特别的地方，对于一些高级课，他只接受其他科的先修，而发出先修课程请求

-那我整理下我接下来要做的事情，用和你学术的模型；更新PageRank的指向算法，余弦相似度大于0.3定为双向指向，然后如果两个边存在先修边，那就额外增加部分指向；用Jaccob分数跑先修；更新推荐分数公式，三者加权

-jaccard 和 去掉语义污染的pagerank有什么不同，然后这里有一个问题，先修课程是单向的，一门高级课可能只会接受指向，并且不去指向别人

-Jaccard 是局部的、对称的——只看 A 和 B 两门课共享多少先修课，不管图的其他部分。比如 15-251 和 21-228 都要求 15-122，Jaccard 就能捕获到。
PageRank 是全局的、传播的——一门课的分数取决于有多少课指向它，以及指向它的课本身有多重要。它反映的是"在整个先修网络里有多核心"，而不是"和某门特定课有多像"。
两者确实不重复，互补的。
你形容下这两个适合什么图，或者说我用这两种方法时，一个适用这两个算法的图分别是什么

-节点有属性集合（不是边），相似度定义在属性重叠上。典型是 user-item 协同过滤——两个用户都买过哪些商品，或者两篇论文引用了哪些相同文献。图结构本身不重要，重要的是节点挂载的集合。
你用ABCD为节点这种字母举个例子

-这个不是我想要的。我们现在已经有了一个先修边的图。我们能不能只通过新修编的传递方向去判断课程的？相似程度。比如说我这里有一个基础集合a。课程b和c都使用了基础集合a那么b和c的相似度是比较高的。如果有一个课程d使用了课程c作为先修。且他使用了另一部分课程d那么课程e那么课程d受到了基础课程。A集合的影响，并且受到了课呃集合课程e的影响，那么d就和e传递的课程。有一部分关系，那么我们就可以通过判断。他们这些课程可能有潜在的关系。我这是语音输入的，你看看你能理解不，这个是要用什么算法

-emmm，得加一个衰减，有点类似神经网络，初级的课到高级后影响肯定是要影响小的，但要有影响，还有防止类似梯度丢失的问题。梯度应该设计成一个方程，类似于exp那种方程，主要是到了一定程度，梯度就不再以一种明显效果下降了，或者说ReLU

-合理，这次很合理。但我们还能从另一个角度看相似度，就是看他作为先修课程的相似度，比如21228和15251可以同时作为21300 21292的先修之一；但15251还能作15451的先修，21228不行，这也是一种相似性的判断。这种算法，怎么设定呢

-先设定一半一半，然后把这个权重放在config里面。回到weight(d) = 0.3 + 0.7 × exp(-d)这个里面，权重衰减有点太快了，我们用线性衰减啊，函数图像左边是水平直线，右边是斜着的直线

-A -> B -> C -> X -> D -> E ->F, A->B表示A是B的先修。离X越远的点，其权重越小，收到的影响越小，这样

-曲线形状对了。阅读上面SPEC0123内容，SPEC_4.1是一个提示词“补丁”或者说“更改提示”，然后根据我们上述置顶的计划，把目前这个算法作为更新补丁写入SPEC里面，记得更改推荐分数公式，还有把相关超参数写入config.py里面。我给Claude Code，让他跑，提示词里面要说写点test case。模型和PageRank的算法部分先不要动


# Claude Code

-阅读SPEC0，根据SPEC0，建立相关文件和相关内容. 

-SPEC0是公约，阅读SPEC0和SPEC1，根据他们的要求写代码

-阅读SPEC0 和 SPEC２，SPEC０是公约，根据这两个文件要求写相关代码

-根据SPEC0 和 SPEC3的要求写代码，SPEC0是公约，SPEC3是任务集相关细节

-阅读SPEC0 和 SPEC4，根据这两个的文件写代码。SPEC0是公约，SPEC4是具体细节

-阅读SPEC_4.1，根据文件要求写入或者更改代码

-阅读SPEC_4.2_ui文件，根据这个文件更改或者写代码

-增加功能，左下角显示的文本，应该根据highlighted courses to show 来决定，也就是有几个高亮的点，就显示几条文本。对于显示文本的窗口不够的情况，增加一个滚动条来显示

-对于图形显示，当我们PCA降低维度后，通过用户选择的点计算他们的几何中心，然后把这个中心作为2D 或者 3D的图像展示的中心点。然后以这个中心点为圆心去扩大半径，并且计算半径里面的点的数量，直到显示了k个点，也就是用户选择的点。如果遇到了平局，我们选择显示课号最小的点。课号是xx-yyy，把他变成xxyyy的一个数，然后选最小，就是这个逻辑。

-阅读SPEC5，根据要求改写代码

-现在更新下diagram显示方式。我们现在的逻辑是不断扩大半径直到包含要显示的点，改成，不断扩大半径，直到包含top k个点，然后从区域里面选取要显示的点，选取规则是从相关性从高到低，直到这个圆里面的点的数量达到的用户选择的(包含top k的那几个高亮点，和用户选择的课程的点)

-图像显示逻辑有问题。我说的算法是以用户输入的点或者多个点的几何中心为图像的中心点，然后选取半径，半径形成的圆要能包含"highlighted coureses to show"上面用户选择的数值。之后在这个圆里，显示top其他的点，直到满足"max nodex to display"的数量要求。其他的点的选取方法就是和我选择的课程相似度从高到低排序这个顺序，直到圆里的nodes数量满足用户输入的数量

-阅读公约SPEC0 和 SPEC_3.1,根据SPEC_3.1的要求来改代码

-再增加一个功能，就是把画图里面计算的几何中心用0.8的亮红色标记在diagram上，RGB是225, 0,0。不要更改其他功能，你做的就是增加一个几何中心。几何中心用亮圆形

-现在graph builder里面是创造了边，余弦相似度用没用课到课程的边，还是只用了向量空间中原点到课程坐标的向量和其他用这种方法组成向量夹角

-阅读SPEC0 和 SPEC5， 根据SPEC5 要求来改写代码

-不对，更改下，用户看到节点大小，以及决定图上哪些点显示应该由recommend score决定，recommend score，虽然还用top k 个节点排序，但这次用recommend score。更改代码之前，你看看在搜索课程栏为空的情况下，图像显示和点的显示是用到了config 的哪些参数

-第一个问题：upstream 和 downstream 有没有分权重？
没有。_weighted_neighborhood 里对 upstream（先修）和 downstream（被以本课为先修的课）的处理代码完全一样，只按 BFS 深度 d 衰减，方向不影响权重。A→B→C 和 C→B→A 算出来的权重值相同。
对于这个问题，你在config里面创造两个变量，一个控制upsteam, 一个控制downsteam。对于图拉说，upsteam and downsteam用的是先修的那张图，pagerank用的是builder的图，对不

-你能给PageRank先修边的强度加一个权重参数吗，然后把这个阻尼系数也作为一个参数，最后把两个参数放到config里面

-存成矩阵，那是不是一个4000*4000的矩阵，因为一共有4000门课。还有，这个能缓存吗。对于相似度和PageRank，这俩能做成矩阵吗，能缓存吗

-不对，你理解错了，PageRank只有当输入课程为空时才应用，而且是只有右测的图。输入课程不为空时，全部使用recommend scores，recommend scores就是我说的3这归一化只和，显示点的逻辑全部都是用recommend score。

-我们现在停一下。我要做出来一下的修改。
1. 撤销模型，把现在的模型变成 all-MiniLM-L6-v2，一个384维度的模型，保持现在的模型储存功能，避免热重载重复加载模型，改变model.py
2. 取消分数min-max归一化，三个信号直接加一起作为recommend score就好了。
3. 强调：现在只有用户输入为空时是使用PageRank显示图形，显示图形逻辑不用变，但改变后的recommend score要同步左侧推荐课程和右侧的图中
4. 清楚分数的缓存数据，以及相关代码，采用实时计算。BFS用numpy计算而不是for loop循环计算，不使用矩阵运算。
更新对应的config， gitignore 等有关相关代码

-20个候选是不对的，我们还需要考虑哪些没有标记为红色显示在图中的灰色点，而且我们也不能保证你选的那几个点加上结构分数后依旧是最高的

-你先写一个小的功能，让图形显示x + y + z 这样的分数，x 表示cosine， y表示结构分，z表示oagerank，都是原始分数，我看看这个阈值应该用哪一个，用多大

-修 build_graph 的 O(N²) Python 循环
这是启动时间的问题，用 numpy 替换双重 for loop，跟查询速度无关。你打算怎么修，从循环到numpy? 不要写代码，说个思路

-现在关注graph里面的的三个py文件，builder, pagerank, prereq。对这三个图实现一个缓存，三个文件，存储三个矩阵，以npy文件结尾，使用numpy 或其他快速处理循环的方法。我的目的就是让文件在启动的时候尽可能快速地计算出分数。先不要改代码，有什么疑问吗

-都存矩阵。prereq能存分数吗，然后对应的recommand也改一下，pagerank 能存分数吗，考虑随机数影响。不用考虑user_courese.json的文件，N不会改变。不要改代码，有什么问题吗

-这是现在的页面，我要在上面的bar (prerequist path 旁边) 增加一个hidden courese选项，用户可以隐藏课程（不改变评分逻辑，只改变graph里面显示的点），交互逻辑和现在search course一样。不要给代码，有什么问题吗

-第三个tab，但是tab顺序是Semantic Graph |  Hidden Course | Prerequisite Path ；只影响sematic graph里的点显示，2D and 3D，隐藏点；列表也照常隐藏；第四个问题是一直隐藏，但图的中心和中心点的判别逻辑不受到影响。总之，隐藏只意味着隐藏点和recommend list，其他算法逻辑都不收到影响。有什么疑问吗

-Prerequisite Path的课程选择，我需要用户能邮件选择文本，然后删除全部，也能左键点击光标位置来进行三处
